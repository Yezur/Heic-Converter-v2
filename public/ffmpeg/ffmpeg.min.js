export const fetchFile = async (file) => new Uint8Array(await file.arrayBuffer());

export const createFFmpeg = () => {
  return {
    isLoaded: () => false,
    load: async () => {
      throw new Error('ffmpeg.wasm assets are missing. Replace /public/ffmpeg/* with the official ffmpeg.wasm files.');
    },
    setProgress: () => undefined,
    FS: () => {
      throw new Error('ffmpeg.wasm assets are missing.');
    },
    run: async () => {
      throw new Error('ffmpeg.wasm assets are missing.');
    },
    exit: () => undefined
  };
};
